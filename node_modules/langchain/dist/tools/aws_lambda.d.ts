import { DynamicTool, DynamicToolParams } from "./dynamic.js";
interface LambdaConfig {
    region: string;
    accessKeyId: string;
    secretAccessKey: string;
    functionName: string;
}
declare class AWSLambda extends DynamicTool {
    private lambdaConfig;
    constructor({ name, description, ...rest }: LambdaConfig & Omit<DynamicToolParams, "func">);
    _func(input: string): Promise<string>;
}
export { AWSLambda };
